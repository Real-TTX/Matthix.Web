services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: matthix-web:latest
    container_name: matthix-web
    ports:
      - "4500:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
    restart: unless-stopped
